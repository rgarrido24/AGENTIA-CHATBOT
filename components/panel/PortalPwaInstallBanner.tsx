'use client';

/**
 * Banner de instalación PWA deshabilitado.
 * El portal no debe mostrar ningún cartel/modal de instalación.
 * Quien quiera notificaciones usa la campana del panel.
 */
export function PortalPwaInstallBanner(_props?: { accent?: string }) {
  return null;
}
